# This project containst the basic and crucial variogram functions for resource estimation in geostatistic calculations.
- Variation of differences in sample values depending on distance is known as <i><b>spatial variation</b></i>. It has a vital importance in evaluating mineral deposits.
- To see the graphs, you need to download "matplotlib" (Type 'python -m pip install matplotlib' in the terminal).
